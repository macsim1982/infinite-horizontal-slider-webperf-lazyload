{
  "name": "infinite-horizontal-slider-webperf-lazyload",
  "version": "1.0.0",
  "description": "A vanilla JavaScript slider that lazy-loads its JavaScript via dynamic import when each slider enters the viewport. The DOM is replaced with lit-html templates and users can swipe or navigate between images.",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "lit-html": "1.4.1",
    "rematrix": "0.7.2"
  },
  "devDependencies": {
    "@parcel/core": "^2.11.0",
    "parcel": "^2.11.0"
  },
  "keywords": []
}
