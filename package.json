{
  "name": "instagram-slider-with-video",
  "version": "1.0.0",
  "type": "module",
  "description": "Vanilla JS PLP slider: bootstrap + lazy chunk on viewport/interaction, event delegation via vanilla-delegate, lit-html rendering, GPU-friendly transforms.",
  "scripts": {
    "generate:stress-grid": "node scripts/generate-stress-grid.mjs",
    "prebuild": "npm run generate:stress-grid",
    "prestart": "npm run generate:stress-grid",
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "lit-html": "1.4.1",
    "rematrix": "0.7.2",
    "vanilla-delegate": "^1.0.1"
  },
  "devDependencies": {
    "@parcel/core": "^2.11.0",
    "parcel": "^2.11.0"
  },
  "keywords": []
}
